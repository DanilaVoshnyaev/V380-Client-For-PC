# Зависимости проекта. Установка всего разом:
#
#     pip install -r requirements.txt
#
# Python 3.12. Для клиента (client.py) дополнительно нужен установленный
# VLC 64-бит — это внешняя программа, не пакет Python, ставится отдельно.

# --- probe.py: опознавание камер и поиск потоков ---
onvif-zeep        # ONVIF: сведения об устройстве, профили, PTZ
WSDiscovery       # поиск ONVIF-камер в сети (WS-Discovery)

# --- client.py: окно просмотра ---
python-vlc        # вывод видео; требует установленного VLC 64-бит

# --- vpn/wg_setup.py: генерация конфигов WireGuard ---
cryptography      # ключи X25519
qrcode[pil]       # QR-коды для телефона: конфиг VPN, ссылка на поток в клиенте

# --- сборка .exe (build.py), нужна только при сборке ---
pyinstaller
